<Window x:Class="AppLauncher.Presentation.WPF.MqttControlWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MQTT 제어 센터"
        Height="600"
        Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="#1E1E1E">

    <Window.Resources>
        <!-- Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#3B82F6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2563EB"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#3F3F46"/>
                    <Setter Property="Foreground" Value="#71717A"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Danger Button Style -->
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#EF4444"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#DC2626"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Success Button Style -->
        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#22C55E"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#16A34A"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="MQTT 제어 센터"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="White"
                       Margin="0,0,0,5"/>
            <TextBlock Text="MQTT 브로커 연결 상태를 확인하고 관리합니다"
                       FontSize="12"
                       Foreground="#71717A"/>
        </StackPanel>

        <!-- Connection Status -->
        <Border Grid.Row="1"
                Background="#2C2C2C"
                CornerRadius="8"
                Padding="15"
                Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="연결 상태"
                               FontSize="12"
                               FontWeight="Bold"
                               Foreground="#71717A"
                               Margin="0,0,0,5"/>
                    <TextBlock x:Name="ConnectionStatusText"
                               Text="연결 안됨"
                               FontSize="16"
                               FontWeight="Bold"
                               Foreground="#EF4444"/>
                    <TextBlock x:Name="BrokerInfoText"
                               Text="브로커: -"
                               FontSize="11"
                               Foreground="#A1A1AA"
                               Margin="0,5,0,0"/>
                    <TextBlock x:Name="ClientIdText"
                               Text="클라이언트 ID: -"
                               FontSize="11"
                               Foreground="#A1A1AA"
                               Margin="0,2,0,0"/>
                    <TextBlock x:Name="TopicText"
                               Text="구독 토픽: -"
                               FontSize="11"
                               Foreground="#A1A1AA"
                               Margin="0,2,0,0"/>
                </StackPanel>

                <Ellipse x:Name="ConnectionIndicator"
                         Grid.Column="1"
                         Width="20"
                         Height="20"
                         Fill="#EF4444"
                         VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Log Area -->
        <Border Grid.Row="2"
                Background="#2C2C2C"
                CornerRadius="8"
                Padding="0"
                Margin="0,0,0,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        Background="#252525"
                        CornerRadius="8,8,0,0"
                        Padding="12,8">
                    <Grid>
                        <TextBlock Text="연결 로그"
                                   FontSize="12"
                                   FontWeight="Bold"
                                   Foreground="#A1A1AA"
                                   VerticalAlignment="Center"/>
                        <Button x:Name="ClearLogButton"
                                Content="로그 지우기"
                                HorizontalAlignment="Right"
                                Style="{StaticResource ModernButton}"
                                Padding="10,5"
                                FontSize="11"
                                Click="ClearLogButton_Click"/>
                    </Grid>
                </Border>

                <ScrollViewer Grid.Row="1"
                              VerticalScrollBarVisibility="Auto"
                              Padding="12">
                    <TextBlock x:Name="LogTextBox"
                               Text="로그가 여기에 표시됩니다..."
                               FontFamily="Consolas"
                               FontSize="11"
                               Foreground="#A1A1AA"
                               TextWrapping="Wrap"
                               Background="Transparent"/>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Control Buttons -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="ConnectButton"
                    Grid.Column="0"
                    Content="연결"
                    Style="{StaticResource SuccessButton}"
                    Margin="0,0,5,0"
                    Click="ConnectButton_Click"/>

            <Button x:Name="DisconnectButton"
                    Grid.Column="1"
                    Content="연결 해제"
                    Style="{StaticResource DangerButton}"
                    Margin="5,0,5,0"
                    IsEnabled="False"
                    Click="DisconnectButton_Click"/>

            <Button x:Name="CloseWindowButton"
                    Grid.Column="2"
                    Content="닫기"
                    Style="{StaticResource ModernButton}"
                    Background="#52525B"
                    Margin="5,0,0,0"
                    Click="CloseWindowButton_Click"/>
        </Grid>
    </Grid>
</Window>
